//boolean based on if the numbers are ordered
function isSorted(array) {
    //is this how you represent an array?
}

//tests to check with for when I finish
isSorted([]) // true
isSorted([-Infinity, -5, 0, 3, 9]) // true
isSorted([3, 9, -3, 10]) // false